sudo: required
services:
  - docker

before_install:
  - docker build -t gjaltreitsma/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true gjaltreitsma/docker-react npm run test -- --coverage
